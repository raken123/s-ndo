<?xml version="1.0" encoding="utf-8"?>
<!-- Hubs are often self-hosted on a LAN without TLS, so plain ws:// must be allowed. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
