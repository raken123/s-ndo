# build scratch
build/.android/
build/.desktop/
build/debug.keystore
dist/shots*/
dist/*.idsig

# server state (VIP grants) is per-deployment
server/state.json

node_modules/
dist/icons/

# the Gemini key a release is built with — never in the repository
build/apikey.txt

# built artifacts carry that key, so they are not committed either
dist/*.html
dist/*.apk
dist/*.deb
dist/*.exe
dist/*.dmg
__pycache__/
__pycache__/
