package com.raken.rakenos.system;

import androidx.core.content.FileProvider;

/** Dedicated provider so update packages can be handed to the Android package installer. */
public class RakenFileProvider extends FileProvider {
}
